
describe('Auth Routes', () => {
  beforeAll(async () => {
  })

  afterAll(async () => {
  })

  beforeEach(async () => {
  })

  describe('POST /upload', () => {
    test('', () => {
      expect(true).toBeTruthy()
    })
  })
})
